#!/bin/bash 

while true 
do
	clear #clear the screen everytime this is run
	./dash.sh #run the script dash.sh
	sleep 1 #run dash - wait one second and go back into the loop - without sleep, the script runs too fast and is unreadable
done

